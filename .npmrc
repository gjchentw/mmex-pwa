# Enforce package.json "engines" instead of merely warning about it.
#
# In CI, setup-node provisions the version from .nvmrc, so this turns `npm ci`
# into the check that .nvmrc still satisfies engines: if the two drift apart,
# the install fails with EBADENGINE rather than silently building on an
# unsupported runtime.
engine-strict=true
